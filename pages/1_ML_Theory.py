import streamlit as st

# ตั้งค่าหน้าเว็บให้ดูสวยงาม
st.set_page_config(page_title="รายละเอียดโมเดล ML - 6704062612049", layout="wide")

st.title("📚 เจาะลึกกระบวนการพัฒนา Machine Learning")
st.write("---")

# 1. ข้อมูลที่นำมาใช้ (Dataset)
st.header("1. ข้อมูลที่ใช้ในโปรเจค (Dataset)")
st.subheader("📌 ที่มาของข้อมูล")
st.write("สำหรับโปรเจคนี้ ผมเลือกใช้ข้อมูลจำลอง (Synthetic Data) ที่สร้างขึ้นมาโดยใช้ Gemini เพื่อจำลองสถานการณ์การพยากรณ์ความเสี่ยงด้านสุขภาพของพนักงานในองค์กร [cite: 4]")

st.subheader("📌 รายละเอียดตัวแปร (Features)")
st.write("ข้อมูลชุดนี้ประกอบด้วยตัวแปรสำคัญ 3 ตัวที่ใช้ในการทำนาย ดังนี้: [cite: 5]")
st.markdown("""
* **Age (อายุ):** ข้อมูลอายุของพนักงานแต่ละคน
* **Cholesterol (คอเลสเตอรอล):** ระดับไขมันในเลือด ซึ่งเป็นปัจจัยหลักของสุขภาพหัวใจ
* **Stress_Level (ระดับความเครียด):** คะแนนความเครียดจากการประเมิน (เรทติ้ง 1-10)
* **Target (ผลลัพธ์):** ตัวชี้วัดว่ามีความเสี่ยง (1) หรือปกติ (0)
""")

# 2. การเตรียมข้อมูล (หัวใจสำคัญของโปรเจค)
st.header("2. การเตรียมข้อมูลก่อนเริ่มสอนโมเดล (Data Preparation)")
st.info("ตามเงื่อนไขของโปรเจค ข้อมูลชุดนี้ถูกออกแบบมาให้มีความไม่สมบูรณ์ เพื่อแสดงทักษะการทำ Data Cleaning ")

st.write("**ปัญหาที่พบและวิธีการแก้ไข:**")
st.markdown("""
1. **จัดการค่าว่าง (Missing Values):** ในข้อมูลดิบจะมีบางแถวที่อายุหรือคอเลสเตอรอลหายไป ผมเลือกใช้วิธีลบแถวที่ไม่สมบูรณ์ออกเพื่อให้โมเดลได้รับข้อมูลที่ถูกต้องที่สุด
2. **กำจัดข้อมูลซ้ำ (Duplicates):** ลบแถวที่มีการกรอกข้อมูลซ้ำซ้อนกัน เพื่อไม่ให้โมเดลจำจำข้อมูลเดิมซ้ำๆ จนเกิดอาการ Overfitting
""")

st.code("""
# ตัวอย่างโค้ดที่ใช้ในการคลีนข้อมูล
import pandas as pd

df = pd.read_csv('datasets/heart_data.csv')
df = df.dropna()           # ตัดแถวที่มีค่าว่างทิ้ง
df = df.drop_duplicates()  # ตัดข้อมูลที่ซ้ำกันทิ้ง
""", language='python')

# 3. ทฤษฎีและอัลกอริทึม
st.header("3. แนวคิดของโมเดล Ensemble (Voting Classifier)")
st.write("ผมเลือกใช้เทคนิค **Ensemble** เพราะเป็นการรวมพลังของหลายอัลกอริทึมมาช่วยกันตัดสินใจ ซึ่งในโปรเจคนี้ประกอบด้วย 3 ประเภทหลัก: [cite: 10, 11]")



st.markdown("""
* **Random Forest:** ใช้หลักการสร้างต้นไม้ตัดสินใจหลายๆ ต้นแล้วเอาผลโหวตส่วนใหญ่มาตอบ
* **Logistic Regression:** ใช้คำนวณหาความน่าจะเป็นว่าข้อมูลนี้อยู่ในกลุ่มไหน
* **SVC (Support Vector Machine):** เน้นการสร้างเส้นแบ่งกลุ่มข้อมูลที่ชัดเจนที่สุด
""")

st.write("**แหล่งอ้างอิงข้อมูล:** กระบวนการพัฒนาอ้างอิงจากคู่มือ Scikit-learn และทฤษฎี Machine Learning เบื้องต้น [cite: 17]")
