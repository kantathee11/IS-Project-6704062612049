import streamlit as st

# ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="รายละเอียดโมเดล ML - 6704062612049", layout="wide")

st.title("📚 ข้อมูลและกระบวนการพัฒนา Machine Learning")
st.write("---")

# 1. ที่มาและลักษณะของข้อมูล
st.header("1. ข้อมูลที่ใช้ในโปรเจค (Dataset)")
st.subheader("📌 ที่มาของข้อมูล")
st.write("ข้อมูลชุดนี้เป็นข้อมูลจำลอง (Synthetic Data) ที่สร้างขึ้นเพื่อใช้ประกอบการศึกษาในวิชา IS 2568 โดยจำลองสถานการณ์การวิเคราะห์ความเสี่ยงด้านสุขภาพของพนักงาน")

st.subheader("📌 รายละเอียดตัวแปรที่สำคัญ (Features)")
st.markdown("""
* **Age (อายุ):** ข้อมูลอายุที่เป็นตัวเลข
* **Cholesterol (คอเลสเตอรอล):** ระดับไขมันในเลือดที่เป็นปัจจัยเสี่ยง
* **Stress_Level (ระดับความเครียด):** ค่าคะแนนความเครียดจากการประเมิน (1-10)
* **Target (ผลลัพธ์):** การจำแนกกลุ่มปกติ (0) และกลุ่มเสี่ยง (1)
""")

# 2. ขั้นตอนการเตรียมข้อมูล
st.header("2. การเตรียมข้อมูล (Data Preparation)")
st.info("เพื่อให้ตรงตามโจทย์ที่ต้องจัดการกับข้อมูลที่ไม่สมบูรณ์ ผมได้ออกแบบ Dataset ให้มีปัญหาเพื่อนำมาผ่านกระบวนการ Clean ข้อมูลดังนี้")

st.markdown("""
* **การจัดการค่าว่าง (Missing Values):** ตรวจเช็คและลบแถวข้อมูลที่ไม่สมบูรณ์ออก เพื่อป้องกันความคลาดเคลื่อนในการพยากรณ์
* **การกำจัดข้อมูลซ้ำ (Duplicates):** ตรวจสอบและลบข้อมูลที่ซ้ำซ้อน เพื่อให้โมเดลเรียนรู้จากข้อมูลที่หลากหลายและไม่เกิดการจดจำรูปแบบเดิมซ้ำๆ (Overfitting)
""")

st.code("""
# ตัวอย่างคำสั่งที่ใช้ในการจัดการข้อมูล
import pandas as pd

df = pd.read_csv('datasets/heart_data.csv')
df = df.dropna()           # ลบค่าว่าง
df = df.drop_duplicates()  # ลบข้อมูลซ้ำ
""", language='python')

# 3. ทฤษฎีอัลกอริทึม
st.header("3. แนวคิดโมเดลแบบ Ensemble (Voting Classifier)")
st.write("ผมเลือกใช้เทคนิค Ensemble ซึ่งเป็นการรวมจุดแข็งของโมเดล 3 ประเภทเข้าด้วยกันเพื่อให้ได้คำตอบที่แม่นยำที่สุด:")

st.markdown("""
1. **Random Forest:** ใช้หลักการโหวตจากต้นไม้ตัดสินใจหลายๆ ต้น
2. **Logistic Regression:** วิเคราะห์หาความน่าจะเป็นในการจำแนกกลุ่ม
3. **SVC (Support Vector Machine):** หาเส้นแบ่งกลุ่มข้อมูลที่เหมาะสมที่สุด
""")

st.write("**แหล่งอ้างอิง:** ขั้นตอนการพัฒนาอ้างอิงจากหลักการใน Scikit-learn และข้อมูลจำลองเพื่อการศึกษา")
