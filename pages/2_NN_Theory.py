import streamlit as st

# ตั้งค่าหน้าเว็บให้สวยงาม
st.set_page_config(page_title="ทฤษฎี Neural Network - 6704062612049", layout="wide")

st.title("🧠 เบื้องหลังการทำงานของโมเดล Neural Network")
st.write("---")

# 1. ข้อมูลที่ใช้พัฒนา
st.header("1. ข้อมูลและตัวแปรที่นำมาใช้")
st.write("ในส่วนของ Neural Network ผมได้นำ Dataset ชุดสุขภาพพนักงานมาทำการทดลองเรียนรู้ครับ เพื่อดูความแตกต่างระหว่างการใช้โมเดลแบบปกติกับโมเดลโครงข่ายประสาทเทียม")
st.write("ตัวแปรหลัก (Features) ที่ป้อนเข้าสู่ระบบ ได้แก่ อายุ, ระดับคอเลสเตอรอล และระดับความเครียด")

# 2. การออกแบบโครงสร้างโมเดล (Model Architecture)
st.header("2. การออกแบบโครงสร้างโครงข่าย (Architecture)")
st.info("เงื่อนไขของโปรเจคคือต้องออกแบบโครงสร้างเอง ผมจึงเลือกใช้ Multi-Layer Perceptron (MLP) ที่ปรับแต่งให้เหมาะกับขนาดข้อมูลครับ")

st.markdown("""
ผมออกแบบเลเยอร์การเรียนรู้ไว้ทั้งหมด 3 ระดับ ดังนี้:
* **Input Layer:** รับค่าจากตัวแปรสุขภาพทั้ง 3 ตัว
* **Hidden Layers:** แบ่งออกเป็น 2 ชั้น โดยชั้นแรกมี 16 โหนด และชั้นที่สองมี 8 โหนด เพื่อเพิ่มความละเอียดในการวิเคราะห์ข้อมูล
* **Output Layer:** สรุปผลลัพธ์ออกมาเป็นค่าความเสี่ยง 0 หรือ 1
""")

# 3. ขั้นตอนการ Training
st.header("3. ขั้นตอนการเรียนรู้ของโมเดล")
st.write("ในการสอนโมเดลชุดนี้ ผมใช้ฟังก์ชันกระตุ้นแบบ 'ReLU' และใช้ตัวปรับจูน Adam Optimizer เพื่อให้ AI เรียนรู้ได้รวดเร็วและแม่นยำที่สุด โดยตั้งค่ารอบการเรียนรู้ไว้ที่ 1,000 รอบเพื่อให้โมเดลมีความเสถียรครับ")

st.header("4. แหล่งอ้างอิงข้อมูล")
st.write("* ข้อมูล (Dataset): จำลองข้อมูลขึ้นเพื่อใช้ในการศึกษาโปรเจควิชา IS 2568")
st.write("* ทฤษฎีอัลกอริทึม: อ้างอิงมาตรฐานจาก Scikit-learn และหลักการ Deep Learning เบื้องต้น")
